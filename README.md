# Packer-Golden-Image-KVM# Packer RHEL Golden Image Builder

Builds a RHEL golden image from a Kickstart meta JSON file.

## Requirements

- Packer installed on the build machine
- QEMU/KVM on the build machine
- HTTP server serving the RHEL DVD tree and Kickstart files